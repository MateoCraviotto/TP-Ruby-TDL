<p id="notice"><%= notice %></p>

<div class="container mx-auto cars--index--catalogue-container">
  <br>
  <h1 class="text-center">Purchase history</h1>
  <p class="text-center lead"><small>These are the cars your have bought.</small></p>
  <br><br>
  
 <% unless @array_four_cars.count == 0 %>
    <% @array_four_cars.each_with_index do |four_cars, j| %>
    
      <%= render partial: 'purchased_cars', locals: {cars: four_cars} %>

      <% if j != @last_index %>
        <br><br><br>
      <% else %>
        <br>
      <% end %>
    <% end %>

  <% else %>
    <p class="text-center lead">You have not bought any car yet.</p>
    <br><br>
  <% end %>

</div>

<br><br>